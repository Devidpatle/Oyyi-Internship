---
const { title,description,image,link} = Astro.props
---
  
<div class="card card-side w-1/3 bg-base-100 shadow-sm hover:shadow-lg hover:resize-y">
  <figure><img src={image} alt={title} class="" /></figure>
  <div class="card-body">
    <h2 class="card-title">{title}</h2>
    <p>{description}</p>
    <div class="card-actions justify-end mt-5">
      <a role="button" href={link} class="btn btn-outline rounded-[0]">Read More</a>
    </div>
  </div>
</div>


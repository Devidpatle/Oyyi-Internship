---
const {title,description, link="", secondBtn=false} = Astro.props;
import video from '../components/video/new_bg.mp4';

---


<section class="hero min-h-screen bg-zinc-900">
    <div class="hero-content ">
        <div class="grid max-w-screen-xl px-4 py-8 mx-auto lg:gap-8 xl:gap-0 lg:py-16 lg:grid-cols-12">
            <div class="mr-auto place-self-center lg:col-span-7">
                <h1 class="max-w-2xl mb-4 text-7xl font-extrabold tracking-tight leading-none text-white ">{title}</h1>
                <p class="max-w-2xl mb-6 font-light text-gray-100 lg:mb-8 md:text-lg lg:text-xl ">{description}</p>
                <a href={link} class="inline-flex items-center justify-center px-10 py-3 mr-3 text-base font-medium text-white text-center bg-primary rounded-[0]">Explore</a>
                {secondBtn && <a href="https://dashboard.oyyi.xyz/register" class="inline-flex items-center justify-center px-10 py-3 text-base font-medium border-primary border-[1px] text-white">
                    Register
                </a>}
            </div>
            <div id="bg-video-container" class=" overflow-hidden w-full lg:mt-0 lg:col-span-5 lg:flex absolute left-0 top-20 object-cover -z-10 " >
                <video autoplay muted loop class=" w-full max-h-screen scale-x-150 brightness-90" >
                    <source src={video} type="video/mp4">
                  </video>            
                </div>
        </div>
    </div>
</section>